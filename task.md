# Stage 3 Build Tasks: Inventory Single-Viewport Taste-Tier Redesign

- [x] 1. Update `src/app/(routes)/inventory/page.tsx` - compact outer padding and convert page title to sleek compact bar.
- [x] 2. Update `src/app/(routes)/inventory/ClientHelper.tsx` - compact main tabs strip to `h-8` and sub-tabs strip to `h-7`.
- [x] 3. Update `src/components/inventory/ProductsTab.tsx` - compact search bar (`h-8 ps-9`) and action buttons (`h-8 px-3`).
- [x] 4. Update `src/components/inventory/ProductsTab.tsx` - standardize dropdown filter triggers to `h-7.5 px-2.5 text-xs font-bold`.
- [x] 5. Update `src/components/inventory/ProductsTab.tsx` - compact table container with `max-h-[calc(100vh-270px)]` and `sticky top-0 z-20` header.
- [x] 6. Update `src/components/inventory/ProductsTab.tsx` - reduce table rows to `px-3 py-1.5`, stock font to `text-xs font-mono font-black`, and compact pagination bar.


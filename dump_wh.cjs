const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const whs = await prisma.warehouse.findMany({ include: { branch: true, _count: { select: { stocks: true, purchaseInvoices: true, stockMovementsFrom: true, stockMovementsTo: true } } } }); console.log(JSON.stringify(whs, null, 2)); } main().catch(console.error).finally(() => prisma.());

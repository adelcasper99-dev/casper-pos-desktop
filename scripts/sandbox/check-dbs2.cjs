const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const branches = await prisma.branch.findMany(); const warehouses = await prisma.warehouse.findMany({ include: { branch: true }}); console.log('BRANCHES:', JSON.stringify(branches, null, 2)); console.log('WAREHOUSES:', JSON.stringify(warehouses.map(w => ({id: w.id, name: w.name, branchId: w.branchId, branchName: w.branch?.name, isDefault: w.isDefault})), null, 2)); } main().catch(console.error).finally(() => prisma.());
